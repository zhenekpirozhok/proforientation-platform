# 📘 Документация по базе данных

**Проект:** Платформа профориентационных тестов (RIASEC + SAP + общие профессии)  
**СУБД:** PostgreSQL  
**Тип БД:** OLTP (транзакционная)

---

# 1. Цели и назначение базы данных

База данных обеспечивает:

* хранение квизов, версий квизов и вопросов;
* хранение ответов пользователей (как авторизованных, так и гостей);
* вычисление и нормализацию трейтов RIASEC;
* хранение рекомендованных профессий;
* локализацию сущностей (переводы);
* управление пользователями и ролями;
* безопасный доступ аналитическим пользователям через отдельные view.

БД построена в 3НФ, поддерживает целостность на уровне ограничений, индексов и бизнес-логики в триггерах.

---

# 2. Логическая схема (общее описание)

Схема состоит из следующих блоков:

### **Справочники**

* `profession_categories` – категории профессий.
* `professions` – полный список профессий.

### **Пользователи и роли**

* `users` — пользователи платформы.
* Пользовательские PostgreSQL-роли (`app_read`, `app_write`, `app_admin`, `app_analytics`) — миграция V7.

### **Квизы**

* `quizzes` — описание квиза.
* `quiz_versions` — версионность.
* `questions` + `question_options` — структура теста.

### **Трейты и вычисление результатов**

* `trait_profiles` — шкалы теста.
* `question_option_traits` — веса влияния.
* `attempts` — попытки.
* `answers` — ответы.
* `attempt_trait_scores` — итоговые трейты.
* `attempt_recommendations` — рекомендации профессий (ML/LLM).

### **Локализация**

* `translations` — многоязычные поля.

### **Views**

* `v_attempt_trait_scores`, `v_attempt_recommendations`,
  `v_attempts_overview`, `v_users_masked`, `v_attempts_overview_masked`,
  `v_quiz_attempts_stats` — миграция V2.

---

# 3. Словарь данных (описание таблиц)

Ниже приведены ключевые таблицы (в соответствии с миграциями V1+V2).

---

## 3.1. profession_categories

| Поле | Тип              | Описание               |
| ---- | ---------------- | ---------------------- |
| id   | serial PK     | Идентификатор          |
| code | varchar(50) uniq | Машинный код категории |
| name | varchar(255)     | Название категории     |

---

## 3.2. professions

| Поле          | Тип                              | Описание            |
| ------------- | -------------------------------- | ------------------- |
| id            | serial PK                     |                     |
| code          | varchar(64) unique               | slug профессии      |
| title_default | varchar(255)                     | Название (по умолч.)|
| description   | text                             | Описание            |
| category_id   | FK → profession_categories.id    |                     |
| ml_class_code | varchar(64)                      | Категория ML-модели |

---

## 3.3. users

| Поле          | Тип               | Описание                |
| ------------- | ----------------- | ----------------------- |
| id            | serial PK      |                         |
| email         | varchar(320) uniq | Нормализуется триггером |
| password_hash | varchar(255)      | BCrypt                  |
| display_name  | varchar(255)      | Отображаемое имя        |
| role          | enum(user_role)   | Роль в системе          |
| is_active     | boolean           | Флаг активности         |
| created_at    | timestamptz       |                         |
| updated_at    | timestamptz       |                         |

**Триггеры:**

* нормализация e-mail — V2;
* обновление updated_at — V2.

---

## 3.4. quizzes

| Поле                    | Тип                        |
| ----------------------- | -------------------------- |
| id                      | serial PK               |
| code                    | varchar(64) unique         |
| title_default           | varchar(255)               |
| status                  | enum(quiz_status)          |
| processing_mode         | enum(quiz_processing_mode) |
| category_id             | FK → profession_categories |
| author_id               | FK → users.id              |
| created_at / updated_at | timestamptz                |

---

## 3.5. quiz_versions

| Поле         | Тип          |
| ------------ | ------------ |
| id           | serial PK |
| quiz_id      | FK → quizzes |
| version      | int          |
| is_current   | boolean      |
| published_at | timestamptz  |

**Ограничения:**

* только одна текущая версия (`UNIQUE WHERE is_current`) — V2;
* `version > 0` — V2.

---

## 3.6. questions

| Поле            | Тип                 |
| --------------- | ------------------- |
| id              | serial PK        |
| quiz_version_id | FK → quiz_versions  |
| ord             | int                 |
| qtype           | enum(question_type) |
| text_default    | text                |

*`text_default` оставлен `text`, так как вопрос может быть достаточно длинным (кейсы, описания ситуаций).*

---

## 3.7. question_options

| Поле          | Тип            |
| ------------- | -------------- |
| id            | serial PK   |
| question_id   | FK → questions |
| ord           | int            |
| label_default | varchar(255)   |

---

## 3.8. trait_profiles

| Поле              | Тип              |
| ----------------- | ---------------- |
| id                | serial PK     |
| code              | varchar(32) uniq |
| name              | varchar(255)     |
| description       | text             |
| bipolar_pair_code | varchar(32)      |

---

## 3.9. question_option_traits

Хранит веса влияния варианта ответа на трейт (0..1).

Ключевые поля:

* `question_option_id` (FK → question_options.id)
* `trait_profile_id` (FK → trait_profiles.id)
* `weight` (numeric / real, диапазон 0..1, CHECK)

---

## 3.10. attempts

Описывает одну попытку прохождения теста (в т.ч. гостевую).

Ключевые поля:

| Поле         | Тип               | Описание                          |
| ------------ | ----------------- | --------------------------------- |
| id           | serial PK      |                                   |
| quiz_version_id | FK → quiz_versions |                               |
| user_id      | FK → users, NULL  | Авторизованный пользователь       |
| guest_token  | varchar(255), NULL| Идентификатор гостя               |
| locale       | varchar(10)       | Язык прохождения (например, `ru`) |
| started_at   | timestamptz       | Начало                            |
| submitted_at | timestamptz       | Окончание                         |

**Ограничения:**

* либо `user_id`, либо `guest_token` (CHECK);
* `submitted_at ≥ started_at`;
* UUID (уникальный индекс в отдельном поле, если используется).

---

## 3.11. answers

| Поле       | Тип            |
| ---------- | -------------- |
| id         | serial PK   |
| attempt_id | FK → attempts  |
| option_id  | FK → question_options |
| ...        |                |

Ограничения:

* вариант ответа должен принадлежать вопросу этого квиза (триггер в V2);
* уникальный ответ для пары `attempt_id + option_id`.

---

## 3.12. attempt_trait_scores

Хранит нормализованные значения 0..1 по каждому трейт-профилю.

Ключевые поля:

* `attempt_id` (FK → attempts.id)
* `trait_profile_id` (FK → trait_profiles.id)
* `score` (0..1, CHECK)

Формируется автоматически триггером после завершения попытки.

---

## 3.13. attempt_recommendations

Список рекомендуемых профессий по попытке.

Ключевые поля:

* `attempt_id` (FK → attempts.id)
* `profession_id` (FK → professions.id)
* `rank` / `score`
* `llm_explanation` (text) — объяснение (может быть длинным, поэтому `text`).

---

## 3.14. translations

Позволяет хранить локализацию сущностей (`title`, `text`, `description` и др.) в одной полиморфной таблице.

| Поле        | Тип                       | Описание                                              |
| ----------- | ------------------------- | ----------------------------------------------------- |
| id          | serial PK              |                                                       |
| entity_type | varchar(32)               | Тип сущности: `'quiz'`, `'question'`, `'question_option'`, `'profession'` |
| entity_id   | bigint                    | Идентификатор сущности в своей таблице               |
| locale      | varchar(10)               | Язык (`'ru'`, `'en'`, `'lt-LT'` и т.п.)              |
| field       | varchar(32)               | Логическое имя поля: `title`, `text`, `description`, `label` |
| value       | text                      | Переведённое значение                                 |

Ограничения:

* `CHECK (entity_type IN ('quiz','quiz_version','question','question_option','profession'))`
* `CHECK (field IN ('title','text','description','label'))`
* `UNIQUE (entity_type, entity_id, locale, field)` — для конкретного поля конкретной сущности на конкретном языке может быть только один перевод.

**Пример соответствия полей:**

* `quizzes.title_default` → `entity_type = 'quiz'`, `field = 'title'`
* `questions.text_default` → `entity_type = 'question'`, `field = 'text'`
* `question_options.label_default` → `entity_type = 'question_option'`, `field = 'label'`
* `professions.title_default` → `entity_type = 'profession'`, `field = 'title'`
* `professions.description` → `entity_type = 'profession'`, `field = 'description'`

---

# 4. Индексы, ограничения, функции и триггеры

Полный список находится в миграции **V2__indexes_triggers_views.sql**.

### 🔹 Функции

* `recalc_attempt_trait_scores` — перерасчёт трейтов.
* `normalize_user_email` — нормализация email.
* `chk_answer_option_belongs_to_question` — проверка корректности ответа.

### 🔹 Триггеры

* пересчёт трейтов при завершении попытки;
* нормализация email;
* защита целостности ответов;
* автоматическое обновление поля `updated_at`.

### 🔹 Views (аналитика и обезличивание)

* `v_attempt_trait_scores`
* `v_attempt_recommendations`
* `v_attempts_overview`
* `v_users_masked`
* `v_attempts_overview_masked`
* `v_quiz_attempts_stats`

---

# 5. Тестовые данные

### Профессии (V3)

* категории профессий;
* ~100+ профессий (генерация slug через регэксп);
* отдельные профессии SAP.

### Пользователи (V4)

* супер-админы;
* админы;
* пользователи;
* все пароли в BCrypt.

### RIASEC (V5)

* трейты R/I/A/S/E/C;
* квиз RIASEC;
* 48 вопросов;
* 5 опций к каждому;
* веса 0 → 1.0;
* заполнение `question_option_traits`.

### Пример попытки + ответы (V6)

* создание попытки;
* автогенерация ответов;
* пересчёт трейтов при завершении;
* добавление рекомендации профессии.

---

# 6. Роли и права доступа (V7)

Полная реализация — миграция `V7__roles_and_permissions.sql`.

### Роли

| Роль            | Права                                 |
| --------------- | ------------------------------------- |
| `app_read`      | только SELECT                         |
| `app_write`     | SELECT, INSERT, UPDATE, DELETE        |
| `app_admin`     | полный доступ (DDL + DML)             |
| `app_analytics` | доступ **только** к обезличенным view |

### Принцип

* приложение работает **под ролью `app_write`** (не superuser);
* аналитики → только view;
* default privileges на будущие объекты настроены.

---

# 7. Транзакции и транзакционность

База данных используется как OLTP-система и соответствует принципам ACID.

## 7.1. ACID

* **Atomicity (атомарность)** — операции можно объединять в транзакции; либо выполняется весь набор, либо ничего.
* **Consistency (согласованность)** — CHECK, NOT NULL, UNIQUE, FK, триггеры и функции гарантируют валидное состояние данных.
* **Isolation (изолированность)** — параллельные транзакции не мешают друг другу (стандартный уровень `READ COMMITTED`).
* **Durability (надёжность)** — зафиксированные транзакции не теряются.

## 7.2. Где в системе используются транзакции (бизнес-операции)

В приложении следующие операции должны выполняться **внутри транзакций**:

### 🔹 Создание квиза

Операция включает:

1. вставку записи в `quizzes`;
2. создание `quiz_versions`;
3. вставку вопросов в `questions`;
4. вставку вариантов ответов в `question_options`;
5. заполнение `question_option_traits`.

Все эти шаги логически образуют одну операцию «создать квиз».  
При ошибке на любом шаге транзакция откатывается, частично созданного квиза не остаётся.

### 🔹 Прохождение теста (attempt)

Операция включает:

1. создание записи в `attempts`;
2. запись ответов в `answers`;
3. пересчёт и запись трейтов в `attempt_trait_scores` (триггер/функция);
4. формирование и запись рекомендаций `attempt_recommendations`.

Эти шаги выполняются как одна транзакция: либо попытка целиком сохранена, либо целиком откатана.

### 🔹 Административные операции

* массовое изменение структуры квиза (добавление/удаление вопросов и опций);
* удаление пользователей или квизов вместе с зависимыми данными (при реализации каскадной логики на уровне приложения).

Такие операции также выполняются как транзакции, чтобы не оставлять «висячие» данные.

## 7.3. Транзакционность миграций

Миграции (`V1__schema.sql`, `V2__indexes_triggers_views.sql` и т.д.) выполняются с помощью миграционного инструмента, который:

* оборачивает каждую миграцию в транзакцию (`BEGIN … COMMIT`);
* при ошибке выполняет `ROLLBACK`, и структура БД остаётся в предыдущем корректном состоянии.

---

# 8. Развёртывание и версионирование

Структура развёртывается через миграции:

* V1 — базовая схема;
* V2 — индексы, ограничения, триггеры, view;
* V3–V6 — сиды (справочники, тестовые данные);
* V7 — роли и permissions.

Миграции хранятся в Git и гарантируют детерминированное создание структуры и возможность отката.
